from setuptools import setup, find_packages

setup(
    name='debjig',
    version='0.0.1',
    description='code tracing, logging, and debug helper',
    author='idrisr',
    author_email='id@hippoplant.com',
    keywords=['debug', 'tracing', 'jig'],
    install_requires=[],
    packages=find_packages(),
    license='Apache 2.0')
